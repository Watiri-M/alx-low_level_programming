#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 * Description: Main
 * Return: Always 0 (Success)
*/

int main(void)
{

	char *s = "main";
	char *f = "testing";
	unsigned int n;

		i = _strspn(s, f);
		printf("%u\n", n);
	return (0);
}
